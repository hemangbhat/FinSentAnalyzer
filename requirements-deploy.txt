# Deployment dependencies — full-featured build.
# All 9 dashboard pages (including FinBERT + the LSTM Stock Prediction
# Extension) work with this set. For a smaller CPU-only image, the Dockerfile
# installs torch from the PyTorch CPU index before installing this file.

# Core ML
scikit-learn>=1.5.0
numpy>=2.0.0
pandas>=2.0.0
joblib>=1.4.0
scipy>=1.13.0

# Deep Learning (FinBERT + LSTM). torch is installed CPU-only in the Dockerfile.
torch
transformers>=4.40.0
accelerate>=0.30.0

# Web App
streamlit>=1.30.0
plotly>=5.20.0
altair>=5.0.0

# Data loading / market data
datasets>=2.18.0
yfinance>=0.2.50

# Explainability & visualization
shap>=0.44.0
matplotlib>=3.8.0

# Utilities
tqdm>=4.66.0
PyYAML>=6.0.0
requests>=2.31.0
